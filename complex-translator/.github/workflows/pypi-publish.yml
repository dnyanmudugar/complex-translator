name: CI & Publish to PyPI

on:
  push:
    branches: [main, master]
    tags: ['v*', '[0-9]*']
  workflow_dispatch:

jobs:
  test_and_build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4
      with:
        submodules: recursive

    - uses: actions/setup-python@v5
      with:
        python-version: '3.10'

    - name: Install build tools & dependencies
      run: |
        python -m pip install --upgrade pip
        python -m pip install build setuptools wheel pytest pytest-custom-exit-code
        # Install your project's runtime requirements if needed by tests:
        # python -m pip install -r requirements.txt 

    - name: Run unit tests
      working-directory: ./complex-translator
      run: |
        python -m pytest --suppress-no-test-exit-code

    - name: Build package
      run: python -m build --no-isolation

    - name: Upload built packages
      uses: actions/upload-artifact@v4
      with:
        name: python-package-distributions
        path: dist/

  deploy:
    runs-on: ubuntu-latest
    needs: test_and_build
    # Only deploy on tag creation (e.g., v1.0.0) or when manually triggered
    if: startsWith(github.ref, 'refs/tags/') || github.event_name == 'workflow_dispatch'
    permissions:
      id-token: write # Required for PyPI Trusted Publishing OIDC
    steps:
    - name: Download built packages
      uses: actions/download-artifact@v4
      with:
        name: python-package-distributions
        path: dist/

    - name: Publish package to PyPI
      uses: pypa/gh-action-pypi-publish@release/v1
